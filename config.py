# -*- coding: utf-8 -*-
"""
差旅搭子 - 全局配置
集中管理应用常量、默认分类、状态枚举与数据目录。
"""
import os

# ---------------------------------------------------------------------------
# 应用元信息
# ---------------------------------------------------------------------------
APP_NAME = "差旅搭子"
APP_VERSION = "1.0.0"
APP_AUTHOR = "差旅搭子团队"

# ---------------------------------------------------------------------------
# 数据目录（SQLite 数据库与附件存储位置）
# 默认放在用户主目录下的隐藏目录，跨平台可用；也可在设置中修改。
# ---------------------------------------------------------------------------
def default_data_dir() -> str:
    base = os.path.join(os.path.expanduser("~"), ".chailv_dazi")
    return base


DATA_DIR = default_data_dir()
DB_PATH = os.path.join(DATA_DIR, "invoices.db")
ATTACHMENT_DIR = os.path.join(DATA_DIR, "attachments")

# ---------------------------------------------------------------------------
# 发票状态枚举
# ---------------------------------------------------------------------------
INVOICE_STATUS = [
    ("draft", "草稿"),
    ("pending", "待报销"),
    ("submitted", "已提交"),
    ("reimbursed", "已报销"),
    ("rejected", "已驳回"),
]

# ---------------------------------------------------------------------------
# 默认发票分类（差旅高频场景）
# 每个分类含：key、显示名、图标 emoji、缺省状态
# ---------------------------------------------------------------------------
DEFAULT_CATEGORIES = [
    ("train", "火车票", "🚄", "pending"),
    ("flight", "机票/行程单", "✈️", "pending"),
    ("hotel", "酒店住宿", "🏨", "pending"),
    ("taxi", "出租车/网约车", "🚕", "pending"),
    ("meal", "餐饮", "🍜", "pending"),
    ("fuel", "加油/过路", "⛽", "pending"),
    ("office", "办公用品", "🖇️", "pending"),
    ("other", "其他", "📄", "pending"),
]

# ---------------------------------------------------------------------------
# 支持的导入来源
# ---------------------------------------------------------------------------
SOURCE_LOCAL = "local"      # 本地文件
SOURCE_EMAIL = "email"      # 邮件
SOURCE_URL = "url"          # 链接
SOURCE_MANUAL = "manual"    # 手工录入

SOURCE_LABELS = {
    SOURCE_LOCAL: "本地文件",
    SOURCE_EMAIL: "邮件导入",
    SOURCE_URL: "链接下载",
    SOURCE_MANUAL: "手工录入",
}

# ---------------------------------------------------------------------------
# 支持的文件扩展名
# ---------------------------------------------------------------------------
SUPPORTED_EXTS = [".pdf", ".png", ".jpg", ".jpeg", ".bmp", ".gif", ".webp", ".ofd"]

# ---------------------------------------------------------------------------
# 常用邮箱 IMAP 主机（用户可选，也可自定义）
# ---------------------------------------------------------------------------
COMMON_IMAP_HOSTS = [
    ("QQ 邮箱", "imap.qq.com"),
    ("网易 163", "imap.163.com"),
    ("网易 126", "imap.126.com"),
    ("Gmail", "imap.gmail.com"),
    ("Outlook/Hotmail", "imap.outlook.com"),
    ("腾讯企业邮", "imap.exmail.qq.com"),
    ("阿里企业邮", "imap.qiye.aliyun.com"),
]
